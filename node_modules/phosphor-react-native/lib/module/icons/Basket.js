"use strict";

/* GENERATED FILE */
import React, { useContext, useMemo } from 'react';
import { IconContext } from '../lib';
import bold from '../bold/Basket';
import duotone from '../duotone/Basket';
import fill from '../fill/Basket';
import light from '../light/Basket';
import regular from '../regular/Basket';
import thin from '../thin/Basket';
import { jsx as _jsx } from "react/jsx-runtime";
function Basket({
  weight,
  color,
  size,
  style,
  mirrored,
  duotoneColor,
  duotoneOpacity,
  ...props
}) {
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle,
    duotoneColor: contextDuotoneColor = '#000',
    duotoneOpacity: contextDuotoneOpacity = 0.2
  } = useContext(IconContext);
  const IconComponent = useMemo(() => {
    const iconWeight = weight ?? contextWeight;
    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin
    };
    return weightMap[iconWeight];
  }, [weight, contextWeight]);
  const mirroredValue = mirrored ?? contextMirrored;
  return /*#__PURE__*/_jsx(IconComponent, {
    color: color ?? contextColor,
    size: size ?? contextSize,
    style: [contextStyle, style, {
      ...(mirroredValue && {
        transform: [{
          scaleX: -1
        }]
      })
    }],
    duotoneColor: duotoneColor ?? contextDuotoneColor,
    duotoneOpacity: duotoneOpacity ?? contextDuotoneOpacity,
    ...props
  });
}
export default Basket;
//# sourceMappingURL=Basket.js.map