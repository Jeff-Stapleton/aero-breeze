/* GENERATED FILE */
import React, { useContext, useMemo } from 'react'
import { type IconProps, IconContext } from '../lib'

import bold from '../bold/XCircle'
import duotone from '../duotone/XCircle'
import fill from '../fill/XCircle'
import light from '../light/XCircle'
import regular from '../regular/XCircle'
import thin from '../thin/XCircle'

function XCircle({ weight, color, size, style, mirrored, duotoneColor, duotoneOpacity, ...props }: IconProps) {
  const {
    color: contextColor = '#000',
    size: contextSize = 24,
    weight: contextWeight = 'regular',
    mirrored: contextMirrored = false,
    style: contextStyle,
    duotoneColor: contextDuotoneColor = '#000',
    duotoneOpacity: contextDuotoneOpacity = 0.2,
  } = useContext(IconContext)

  const IconComponent = useMemo(() => {
    const iconWeight = weight ?? contextWeight

    const weightMap = {
      bold,
      duotone,
      fill,
      light,
      regular,
      thin,
    }

    return weightMap[iconWeight]
  }, [weight, contextWeight])

  const mirroredValue = mirrored ?? contextMirrored

  return (
    <IconComponent
      color={color ?? contextColor}
      size={size ?? contextSize}
      style={[
        contextStyle,
        style,
        {
          ...(mirroredValue && {
            transform: [{ scaleX: -1 }],
          }),
        },
      ]}
      duotoneColor={duotoneColor ?? contextDuotoneColor}
      duotoneOpacity={duotoneOpacity ?? contextDuotoneOpacity}
      {...props}
    />
  )
}

export default XCircle
  